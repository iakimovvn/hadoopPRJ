spring:
  application:
    name: hadoopprj
  cloud:
    config:
      uri: http://localhost:8888
  profiles:
    active: production

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-server:8761/eureka
    instance:
    preferIpAddress: true